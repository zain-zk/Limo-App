{
  "name": "limo-app",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix react\" \"npm run dev --prefix node\"",
    "dev:react": "npm run dev --prefix react",
    "dev:node": "npm run dev --prefix node",
    "build": "npm run build --prefix react",
    "install:all": "npm install --prefix react && npm install --prefix node"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
